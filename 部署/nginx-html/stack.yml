version: "3.9"
services:
  nginx-html:
    image: nginx
    ports:
      - 20010:20010
    environment:
      # 最好使用此设定时区，其它镜像也可以使用
      - TZ=CST-8
    volumes:
      # 配置文件目录
      - /usr/local/nginx-html/conf/:/etc/nginx/conf.d/
      # 网页存放目录
      - /usr/local/nginx-html/html:/usr/share/nginx/html
    deploy:
      replicas: 1

networks:
  default:
    external: true
    name: rebue
