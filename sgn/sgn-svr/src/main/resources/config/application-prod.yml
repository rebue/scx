# 配置中心中的配置
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/sgn?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: sgn
    password: sgn
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    virtual-host: /
  redis:
    password: xxxxxx
    sentinel:
      # Redis服务器名称
      master: redis-server 
      # 哨兵节点 
      nodes:
        - xxx.xxx.xxx.xxx:xxxx
        - xxx.xxx.xxx.xxx:xxxx
