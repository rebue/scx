# 生成环境的配置
server:
  port: 10085

logging:
  config: config/log4j2.xml

spring:
  cloud:
    nacos:
      config:
        server-addr: 122.9.104.17:8848
      discovery:
        server-addr: 122.9.104.17:8848
    sentinel:
      enabled: false
