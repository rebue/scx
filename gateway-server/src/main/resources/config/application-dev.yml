# 配置中心的配置

scx:
  gateway:
    # 发送消息的超时时间(记录请求和响应日志时使用)
    send-timeout: 1000

spring:
  cloud:
    gateway:
      discovery:
        locator:
          filters:
            - name: SignPre
              args:
                filter-urls:
                  - "'/rac-svr/**'"
                ignore-urls:
                  - "'POST:/rac/sign-in/**'"
